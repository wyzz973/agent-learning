# D05 · w04 · 隔天重写与第一周验收

## 今天要做什么

**关掉 ex1/ex2，新建 `weeks/w04-python-tools/recall.py`，凭记忆重写 `find_matches` 和 `make_hits`，用两条新的文件记录验证。**

三件事按顺序做完，到点就停：

1. 重写这两个函数（不看原文件，可以看自己的报错）
2. 填 `weeks/w04-python-tools/README.md` 里 Use It 那张对比表
3. 跑 `./check.sh --week w04`

## 为什么这样做

测试变绿只说明代码能跑，可能是照着范例改出来的。换掉数据、关掉范例再写一遍，才知道是不是真的会。

## 核心思想

迁移到新数据比重复运行旧代码更能发现卡点。

## 能学到什么

不看范例组织出 `for` → `if` → `append` → `return` 这条链，并说清每个函数收什么、返回什么。本周的 Python 写法只有列表、字典、`for`/`if`、`in`/`or`、`append`、`return`。

## 前置条件

D01～D04 的四个函数都已实现且测试通过。

## 60 分钟步骤

流程与时间分配见 [导师执行手册](../TEACHING.md#每日-60-分钟)，今天的具体内容是：

| 阶段 | 今天做什么 |
|---|---|
| 回忆 5 分钟 | 不看代码，说出 `find_matches` 的输入形状和返回形状 |
| 热身 10 分钟 | 重读 `00_warmup.py` 第 1～3 节，只预测不运行 |
| 自己写 30 分钟 | 在 `recall.py` 里重写两个函数，用下面的新数据跑通 |
| 验收 10 分钟 | 跑周检查，填 Use It 表 |
| 交接 5 分钟 | 在学习状态里写下证据和卡点 |

今天用的新数据（不要用 `sample_repo.py` 里的）：

```python
RECORDS = [
    {"path": "app/cache.py", "content": "Cache lookup helper."},
    {"path": "docs/cache.md", "content": "How caching works."},
]
```

开始/检查命令（仓库根目录）：

```sh
uv run python weeks/w04-python-tools/recall.py
./check.sh --week w04
```

## 验收标准

- [ ] `recall.py` 里的两个函数是自己写的，没有复制 ex1 的代码。
- [ ] 用上面两条新记录，关键词 `cache` 能返回两条，关键词 `retry` 返回空列表。
- [ ] 能说出重写时卡住的那一步是取值、循环、收集还是返回。
- [ ] README 的 Use It 表已填，写清框架替你做了哪一步。
- [ ] `./check.sh --week w04` 通过。

## 卡住怎么办

先说清卡在数据形状、语法、调用还是返回，给出最小输入、预期输出和第一条报错。导师先给方向或相似小例子，不直接给实现。到 60 分钟仍未写完就续做本卡，不叠加 D06。

## 资料与边界

- 本周三个薄弱点：列表与元素分层、接住返回值、`return` 的位置。重写时对着这三条自查。
- Python 写法查本周 [热身](../../weeks/w04-python-tools/00_warmup.py) 或 [速查卡](../../SYNTAX_CARDS.md)，每次不超过 10 分钟。
- 通用规则见 [根规则](../../AGENTS.md)，不在本卡重复。
