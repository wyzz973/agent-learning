# D32 · w10 · 用官方 SDK 暴露同一个工具

## 今天要做什么

核查 SDK 安装版本；用 stdio 暴露搜索；日志走 stderr。

产物/编辑位置：`mcp_server.py（教师连接/本人改说明）`。该周代码由导师在开周时按此契约准备，尚未生成；这里已经规定学习动作与验收，不把它说成可运行。

## 为什么这样做

一份能力应能被不同客户端复用。

## 核心思想

协议适配包装既有函数，不复制搜索逻辑。

## 能学到什么

Python：进程、stdin/stdout、异步上下文、装饰器复习。Agent 能力：用官方 SDK 暴露同一个工具。只学习本任务用到的写法；术语到[词典](../../GLOSSARY.md)，接口到[API 手册](../../API_GUIDE.md)按词查。

## 前置条件

完成 [D31](D31.md) 的必要验收；不懂就接着原任务练。

## 60 分钟步骤

1. **5 分钟**：不看答案说出上次任务的输入、输出和一个失败情况。
2. **10 分钟**：读本课所需热身/示范，先预测输出，再运行；让导师分清语法与 API。
3. **30 分钟**：核查 SDK 安装版本；用 stdio 暴露搜索；日志走 stderr。每次只改一小处、运行一次。
4. **10 分钟**：按下面验收核对结果，用自己的话说明“哪个数据交给了谁”。
5. **5 分钟**：在学习状态留下实际证据、卡点与下一步；问答由模型逐字记录。

开始/检查命令（仓库根目录）：

```sh
uv run python scripts/learn.py --task D32
```

这条命令先显示任务契约；该周教材生成后，导师要在当周 README 给出并实测具体运行与测试命令。

## 验收标准

- [ ] 服务启动；tools/list 看见参数 schema；stdout 无调试杂音。
- [ ] 本人能解释核心思想，指出一个容易写错的地方。
- [ ] 状态记录使用实际结果，未完成/未运行明确标注。迁移或重写需要本人动手。

下次学习开头回忆本课，不以提前看完下一课代替本课验收。

## 卡住怎么办

先定位卡在数据形状、语法、调用还是返回；展示最小输入、预期输出和第一条错误。让导师先给方向或相似例子，保留自己的实现机会。60 分钟到仍未完成就续做此任务，不叠加下一课。

## 资料与边界

- [MCP 官方架构](https://modelcontextprotocol.io/docs/learn/architecture)：本次链接跳转到 2026-07-28 版本架构页；具体协议和 SDK 开周再次核查兼容性。
- [MCP Python SDK](https://github.com/modelcontextprotocol/python-sdk)：D32 开周按实际 SDK 版本实现并测试 stdio，不把工具 schema 测试当调用成功。
- [Agent Skills 官方说明](https://agentskills.io/home)：按需加载流程说明与参考材料；不等同于执行工具，也不授予系统权限。

资料查阅每次不超过本任务的 10 分钟；不要同时刷完整课程。本卡的时间分配是针对你的基础与工作时间作出的教学安排，不是来源承诺的学习速度。
