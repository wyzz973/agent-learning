# 术语表

Agent 领域黑话密度极高，很多词的通俗说法和实际所指有偏差。**随学随填，一周至少加两条。**

写法：左栏是大家挂在嘴上的说法，右栏是它实际指什么。右栏要具体到"由谁、在什么时候、做了什么"。

| 术语 | 大家怎么说 | 实际是什么 |
|---|---|---|
| Agent | "会自己思考的 AI" | 一个循环：模型输出 tool_call → 你的代码执行 → 结果追加进消息列表 → 再次调用模型，直到不再返回 tool_call 或撞上轮次上限 |
| Tool calling | "让 AI 调用函数" | 模型只输出一段声明了函数名和参数的 JSON，**执行完全由你的代码完成**。模型从不真的运行任何东西 |
| ReAct | "推理 + 行动的高级模式" | 上面那个循环本身。名字来自论文，不是某个框架的特性 |
| Context engineering | "写好 prompt" | 决定每一轮往上下文里放什么：历史、检索结果、工具定义、工具返回值。是每轮都在做的取舍，不是一次性写死的 prompt |
| Workflow vs Agent | 常被混着叫 | Workflow：LLM 调用次数和顺序由你的代码定死，可预估延迟和成本。Agent：由模型自己决定下一步，灵活但不可预估。**大部分场景 workflow 就够** |
| Memory | "让 AI 记住我" | 两个不同的东西：短期＝当前会话的消息列表（受 context window 限制）；长期＝你主动写进外部存储、下次主动取出来拼进上下文的数据。模型自己什么都不记 |
| RAG | "给 AI 喂知识库" | 检索一段文本拼进上下文。**agentic RAG** 是把检索包装成工具让模型自己决定要不要搜，和固定的"检索→拼接→回答"流水线不是一回事 |
| Eval | "测试 AI 效果" | 一个固定输入集 + 打分器，每次改动后重跑并对比分数。没有固定数据集的"感觉变好了"不是 eval |
| MCP | "AI 的 USB 接口" | 一个 JSON-RPC 协议，把工具放进独立进程，任何兼容客户端都能连。解决的是跨应用复用和进程隔离，不是让工具变强 |
