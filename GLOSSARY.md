# Agent 与 Python 术语词典

按今天遇到的词查，不需要背整张表。每个词都要能在自己的应用中指出对应数据或动作；API 参数示例见 [接口手册](API_GUIDE.md)。

| 名词 / 中文 | 本项目里具体指什么 | 容易混淆的地方 |
|---|---|---|
| list / 列表 | FILES 保存一批记录；循环每次取出一项 | FILES 不是一条文件，不能用 FILES["path"] |
| dict / 字典 | 一条文件的 path、content 键及其值 | 键名不等于变量名；取错层级会出错 |
| function / 函数 | 接收参数、执行动作、return 结果的一段代码 | print 给人看，return 才交给调用方 |
| parameter / 参数 | 函数定义中接收输入的名字 | 传入值与参数名不是同一回事 |
| return / 返回 | 把结果交回调用处，并结束此次调用 | 在循环里过早 return 会只处理第一项 |
| type hint / 类型注解 | list[str] 描述预期形状，帮助检查 | 注解本身一般不校验运行时数据 |
| async/await / 异步 | await 等一次模型或工具调用完成，允许事件循环处理别的工作 | 写 async 不自动让多次调用并行 |
| exception / 异常 | 文件不存在、模型超时等失败信号 | 吞掉异常、返回空结果会混淆失败与无命中 |
| JSON | 跨进程/API 传递数据的一种文本格式 | Python 字典不是 JSON 字符串；True/None 也不按 Python 字面量发送 |
| API / 应用接口 | 约定“传什么、得到什么、失败怎样表示”的调用入口 | API 不一定是网络 HTTP，也可为本地库函数 |
| schema / 数据结构契约 | 工具 keyword 要字符串、回答 paths 要列表 | 结构正确不保证事实正确 |
| LLM / 大语言模型 | 接收上下文并产生下一条模型消息的组件 | 它生成工具调用描述，不会直接执行 Python |
| tool / 工具 | 搜索或读取文件的可执行函数及输入说明 | 名字与 schema 只描述能力，实际函数才执行 |
| tool calling / 工具调用 | 模型声明 name/args/id，程序执行并回传对应结果 | 仅打印 tool_calls 还未完成行动 |
| tool_call_id | 把一次工具结果关联到模型那次请求的编号 | 不是用户会话 ID，也不是工具名字 |
| message / 消息 | human、ai、tool 等角色的历史条目 | AI 消息带工具调用时正文可以为空 |
| agent loop / 执行循环 | 模型选择动作→工具执行→结果回传→再决定 | 必须有终止和错误路径；不限轮次会失控 |
| workflow / 工作流 | 代码预先规定大部分步骤和分支 | 代码循环也能表达分支；有图不代表必须多 agent |
| ReAct | 推理与行动交替、根据观察继续的常见组织方式 | 不要求把模型隐藏推理打印出来，也不是某个 Python 类名 |
| harness / 运行支撑 | 管模型调用、工具路由、状态、权限、预算与验证的环境和程序 | 一个 prompt 文件只是其中一部分 |
| LangChain | 提供模型、工具和常见 agent 连接的库 | 不替你写业务规则，也不保证结果准确 |
| LangGraph | 用共享状态、节点、边与检查点组织可控执行 | 节点可以是普通函数；不是每一步都要调模型 |
| state / 状态 | 当前 query、answer、messages 等数据 | 状态不只聊天历史；字段更新规则需明确 |
| node / 节点 | 读取状态并返回更新的函数 | 注册节点传函数对象，不要提前把函数执行了 |
| edge / 边 | 固定或条件式决定下一个节点 | 边不负责检索正文，实际工作在节点 |
| checkpointer / 检查点保存器 | 保存某个会话执行中的状态与位置 | InMemorySaver 只在本进程内；真正持久化要外部存储 |
| interrupt / 暂停点 | 等外部输入后再恢复的执行位置 | 恢复会重新进入节点，前置副作用可能重跑 |
| idempotency / 幂等 | 同一操作重复提交也不会多生成一份效果 | 重试次数受限与幂等是不同问题 |
| streaming / 流式输出 | 逐次收到节点更新、消息或文本片段 | 打印进度不等于最终任务成功 |
| retrieval / 检索 | 从外部资料中选出有关片段 | 相似内容未必支持回答，必须保留原文来源 |
| RAG / 检索增强生成 | 先取有关证据，再把证据用于生成回答 | 不等于训练模型，也不必一开始就上向量库 |
| embedding / 向量表示 | 把内容映射到可比较的数值表示 | 向量近不等于逻辑成立、事实正确 |
| context engineering / 上下文管理 | 决定本轮给模型哪些信息以及怎样组织 | 不是把所有资料堆进去，也不只是改 system prompt |
| memory / 记忆 | 主动保存并在需要时取回状态或长期信息 | 不保证模型永久记住；记什么、何时读写需设计 |
| structured output / 结构化输出 | 按 schema 产生并解析模型最终答案 | 工具返回字典是另一个环节，二者不能混说 |
| MCP | 让应用与工具服务交换能力和调用消息的协议 | 不负责模型决策，不自动提供权限或业务实现 |
| host/client/server | host 是使用工具的应用；client 管连接；server 暴露能力 | 不是三个必须在三台机器上的程序 |
| stdio / 标准输入输出 | 本地进程间传 MCP 消息的一种传输 | stdout 混入调试文字会污染协议；日志用 stderr |
| Skill | 按需读取的任务方法、步骤和参考材料 | 不等于执行函数，不会自动授予文件或网络权限 |
| planner / 规划器 | 把目标拆成有依赖与完成条件的步骤 | 写一份漂亮计划不等于步骤已经执行 |
| supervisor / 主管模式 | 把任务分给子 agent 并收回结果汇总 | 主管要核验结果，不能把子 agent 自述当证明 |
| handoff / 控制权交接 | 把后续会话或任务控制交给另一角色 | 与普通调用后返回主管不同 |
| sandbox / 沙箱 | 限制代码/工具能访问的环境 | prompt 中“不要越界”不是系统隔离 |
| prompt injection / 指令注入 | 不可信正文尝试改变系统行为 | 文档里的命令是待分析数据，不能覆盖用户和仓库规则 |
| eval / 评估 | 固定任务、实际运行与明确评分规则 | 一次“感觉不错”或一个单测不代表整体质量 |
| trace / 执行轨迹 | 模型与工具步骤、时间、输入输出等观察记录 | trace 展示发生什么，不自动判断对不对 |
| regression / 回归 | 新改动破坏了以前已通过的行为 | 要用旧用例重新执行发现，而非靠记忆 |
| deployment / 部署 | 在可重建的独立环境提供应用入口 | 本机脚本跑通不等于服务已上线 |
